name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: pnpm/action-setup@v5
      with:
        run_install: true
    - run: pnpm install
    - run: pnpm lint
    - run: pnpm build
